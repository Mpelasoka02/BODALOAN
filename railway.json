{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "mkdir -p storage/framework/{cache,sessions,views} storage/logs && chmod -R 775 storage bootstrap/cache && php artisan config:cache && php artisan route:cache && php artisan view:cache && vendor/bin/heroku-php-apache2 public/",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}

